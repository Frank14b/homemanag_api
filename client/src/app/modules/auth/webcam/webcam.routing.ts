import { Route } from '@angular/router';
import { WebcamComponent } from './webcam.component';

export const authWebcamRoutes: Route[] = [
    {
        path     : '',
        component: WebcamComponent
    }
];
